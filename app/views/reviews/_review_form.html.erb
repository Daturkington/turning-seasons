<%= simple_form_for [@recipe, Review.new] do |f| %>
  <%= f.collection_radio_buttons :rating, [[5], [4], [3], [2], [1]], :first, :last %>
  <%= f.input :content, label: 'What did you think?' %>
  <%= f.button :submit, 'Confirm', class: "btn btn-confirm" %>
<% end %>
